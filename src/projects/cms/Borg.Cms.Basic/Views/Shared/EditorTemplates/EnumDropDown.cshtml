@using Borg.MVC.Services.Editors
@model EnumDropDown
<input type="hidden" asp-for="HtmlId" />

<div class="form-group">
    <label asp-for="@Model"></label>
    <select id="@Model.HtmlId" class="form-control" asp-for="@Model.Value" asp-items="@Model.DropDownItems()" style="width: 100%;"></select>
    <span asp-validation-for="@Model"></span>
</div>
<script type="text/javascript" device id="@(Model.HtmlId)_script" script-position="Bottom" weight="101">
    $(function() {
        $("#" + '@Model.HtmlId').select2({
            width: 'resolve',
            tags: false,
            multiple: false,
            placeholder: 'Select an option',
            //theme: "bootstrap"
        });
    });
</script>