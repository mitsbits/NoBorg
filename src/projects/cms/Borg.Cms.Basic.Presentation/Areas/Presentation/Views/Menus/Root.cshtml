@foreach (var section in Orchestrator.Device.Sections)
{


    DefineSection(section.Identifier, async () => await Output.WriteAsync((await Html.RenderViewSection(Component, section)).ToString()));

}